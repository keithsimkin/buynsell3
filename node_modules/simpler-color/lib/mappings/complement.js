"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

exports.__esModule = true;
exports.default = void 0;

var _rotation = _interopRequireDefault(require("./rotation"));

var complement = function complement(baseColor, key) {
  if (typeof key !== 'number' || key === 0) return baseColor;
  var angle = 0;
  var direction = key < 0 ? -1 : 1;
  var i = 0;

  while (i !== key) {
    i += direction;
    if (i % 2 !== 0) angle += 180 * direction;else angle += 30 * direction;
  }

  return (0, _rotation.default)(baseColor, angle);
};

var _default = complement;
exports.default = _default;